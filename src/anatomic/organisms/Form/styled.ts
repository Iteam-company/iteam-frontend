import { Form } from "formik";
import styled from "styled-components";

export const StyledForm = styled(Form)`
    box-shadow: 0px 4px 20px 0px #d0c9d7;
    padding: 35px 45px;
    border-radius: 16px;
    z-index: 10;
    background-color: #fefdff;
`;
export const Icon = styled.img`
    width: 100%;
`;
