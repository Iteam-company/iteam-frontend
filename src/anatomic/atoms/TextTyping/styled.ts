import ReactTypingEffect from "react-typing-effect";
import styled from "styled-components";

export const StyledTypinEffect = styled(ReactTypingEffect)`
    .cursor {
        font-size: 50px;
        font-weight: 100;
    }
`;
