import { COLORS } from "@/lib/theme/color";
import styled from "styled-components";

export const Divider = styled.div`
    height: 0px;
    border: 1px solid ${COLORS.textMinor};
    width: 100%;
`;
